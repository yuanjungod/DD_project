# Tool config catalog — file-backed source of truth for the platform.
# ReAct agents use AgentScope built-ins (view_text_file, execute_python_code, execute_shell_command).
# Prior-agent handoff is passed via output_dir paths and inlined README.md in prompts.

tools: {}
