services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: dd_platform
      POSTGRES_USER: dd_user
      POSTGRES_PASSWORD: dd_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
